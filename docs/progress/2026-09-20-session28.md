# Session 28 — Voice Design Default + Tool Expansion + Notes System
**Fecha:** 2026-09-20 21:02 (America/Santiago)
**Agente:** Luna (cron autónomo)

## Contexto
- Servicio corriendo en :8765 (sesión anterior)
- 497/497 tests pasando al inicio
- Session 27: E2E verification, voice design discovery, slang cleanup
- Disco F: murió el 20 sep 2026, proyecto migrado a D:
- Recomendación principal de session 27: cambiar default TTS a voice design

## Tareas Completadas

### 1. ✅ Voice Design como TTS Default
**Cambio principal de la sesión.** Implementación:

- `config.yaml`: Nuevo parámetro `voice.default_tts: "design"` (options: design, clone, standard)
- `main.py`: Variable `tts_use_design` y `tts_use_clone` leídas desde config
- WebSocket handler: `speak_async()` ahora usa `use_design=tts_use_design` por defecto
- **Voice design produce mejor calidad que voice clone** (descubierto en session 27)
- Voice clone queda como fallback configurable

### 2. ✅ Tool Calling — Reminder y Notes
**Nuevas herramientas implementadas:**

#### `reminder` (ya definida en LUNA_TOOLS, faltaba handler)
- Handler `_reminder()` en allowlist.py
- Parsea tiempo ISO 8601, calcula delay, crea threading.Timer
- Ejecuta `_notify()` al disparar
- Validación: mensaje requerido, tiempo no puede ser pasado

#### `notes` (nueva — herramienta completa)
- Tool definition en `LUNA_TOOLS` (mimo_client.py)
- Handler `_notes()` en allowlist.py
- 4 acciones: `save`, `list`, `read`, `delete`
- Almacena notas como markdown en `memory/notes/`
- Títulos sanitizados como filenames
- Timestamps automáticos en save
- Límite de 5000 chars por lectura
- Security: validación de títulos vacíos

**Allowlist actualizada:** 16 herramientas (antes 14)
```
systeminfo, screenshot, listdir, openapp, volume, datetime, timer,
websearch, clipboard, readfile, webfetch, processes, notify,
weather, reminder, notes
```

### 3. ✅ Tests Actualizados
**10 nuevos tests añadidos a `test_allowlist.py`:**

- `TestReminderCommand`: 4 tests
  - Requiere mensaje
  - Tiempo inválido rechazado
  - Tiempo pasado rechazado
  - Set exitoso con datos correctos

- `TestNotesCommand`: 6 tests
  - Lista notas
  - Save requiere título
  - Save + Read round-trip
  - Read nota inexistente
  - Delete nota inexistente
  - Acción inválida

**Tests totales: 507/507 pasando** ✅ (497→507)

### 4. ✅ MiMo Desktop Prompts
Script `tools/mimo_prompts.py` ejecutado — 5 prompts generados:
- `diagrama-arquitectura.md`
- `mockup-feature-actual.md`
- `banner-release.md`
- `comparativa-ai-assistants.md`
- `roadmap-visual.md`

Guardados en `assets/prompts/` con `index.json`.

## Archivos Modificados
- `python-service/config.yaml` — `voice.default_tts: "design"`
- `python-service/main.py` — TTS default mode desde config
- `python-service/brain/mimo_client.py` — Tool definition `notes`
- `python-service/tools/allowlist.py` — Handlers `reminder` + `notes`, allowlist expandida
- `python-service/tests/test_allowlist.py` — 10 nuevos tests

## Estado del Sistema
- **Tests:** 507/507 pasando ✅
- **RAG:** 522 chunks, semantic enabled
- **Servicio:** Healthy, :8765
- **Voice Design:** ✅ NUEVO DEFAULT — calidad superior a voice clone
- **Tool Calling:** 16 herramientas (antes 14)
- **Notas:** Sistema de notas markdown funcional

## Descubrimientos / Notas
- Voice design (`mimo-v2.5-tts-voicedesign`) ya estaba implementado desde session 27 pero no era el default
- El cambio a default es puramente config + main.py — el código de TTS ya soportaba ambos modos
- Las notas se guardan como markdown legible por el RAG semántico (integración natural)
- El reminder usa threading.Timer — no persiste entre reinicios del servicio (limitación conocida)

## Recomendaciones para Sesión 29
1. 🎤 **Probar ASR con audio real** — El módulo STT está listo pero no se ha testado con micrófono real
2. 🔊 **Testear voice design con Nicolas** — Pedir feedback sobre el perfil de voz (anime_es, joven_latina, calm_assistant)
3. 🖥️ **Verificar Electron overlay** — Asegurar que el dashboard y voice design funcionan juntos
4. 💾 **Persistir reminders** — Usar archivo JSON en lugar de solo threading.Timer
5. 📊 **Dashboard mejorado** — Mostrar voice design stats y notas recientes
6. 🧠 **RAG sobre notas** — Las notas ya se guardan en markdown, verificar que el RAG las indexa
